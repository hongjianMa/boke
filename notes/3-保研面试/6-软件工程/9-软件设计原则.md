# 软件设计原则

## 什么是抽象？

抽象是忽略非本质细节，只保留对象或系统最重要特征的过程。软件设计中通过抽象降低系统复杂度。

## 什么是信息隐藏？

每个模块隐藏自己的内部数据和实现细节，只通过规定的接口与其他模块通信。

## 什么是模块化？

将复杂系统划分为多个相对独立、功能明确的模块，使系统更容易开发、测试、维护和复用。

## 为什么强调高内聚、低耦合？

高内聚：一个模块内部的功能联系紧密，专注完成一项明确任务；
低耦合：不同模块之间依赖较少。