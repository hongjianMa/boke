# 高内聚低耦合

高内聚：一个模块只负责一类高度相关的事情。

低耦合：模块之间依赖尽量少，一个模块的修改不会大量影响其他模块。

## 抽象
抽象是从具体事物中提取共同的、本质的特征，忽略不重要的实现细节，只保留使用者需要关注的内容。

简单理解：

**抽象关注“做什么”，隐藏“具体怎么做”**。

比如开车时，驾驶员只需要知道：

方向盘控制方向。
油门控制速度。
刹车用于减速。

驾驶员不需要了解发动机内部如何燃烧、变速箱如何传动。这就是抽象：对外提供简单的操作方式，隐藏内部复杂实现。